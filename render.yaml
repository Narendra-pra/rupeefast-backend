services:
  - type: web
    name: rupeefast-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
    plan: free
    autoDeploy: true
